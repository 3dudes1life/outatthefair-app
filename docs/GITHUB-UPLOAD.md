# GitHub Upload — V0.5

Use `outatthefair-app-v0.5-github-upload.zip`.

1. Unzip it.
2. Open the unzipped folder.
3. In the `outatthefair-app` repository, choose **Add file → Upload files**.
4. Drag everything inside the folder into GitHub.
5. Replace matching files and commit to `main`.

The upload package excludes `.github`, so it does not replace or remove the existing GitHub Pages workflow.
